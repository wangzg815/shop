spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://172.18.34.126:3306/mall_pms
    driver-class-name: com.mysql.cj.jdbc.Driver


mybatis-plus:
  global-config:
    db-config:
      id-type: auto   #设置主键自增自动
  mapper-locations: classpath:/mapper/**/*.xml

server:
  port: 10000